[package]
name = "prelude"
version = "0.1.0"
authors = ["Julien Cretin <cretin@google.com>"]
license = "Apache-2.0"
publish = false
edition = "2021"

[dependencies]
api = { path = "../api", features = ["wasm"] }
const-default = "1.0.0"
rlsf = "0.2.1"

[features]
multivalue = ["api/multivalue"]
